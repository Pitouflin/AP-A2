{% extends 'base.html.twig' %}
{% block body %}

    <h1>Création d'une commande</h1>

    {{ form_start(form) }}

    {{ form_widget(form) }}

    <button type="submit" class="btn btn-dark">Finaliser la commande</button>

    {{ form_end(form) }}

{% endblock %}